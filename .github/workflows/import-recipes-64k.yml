name: "[ARQUIVADO] Análise do dataset 64k"

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow arquivado
        run: |
          echo "Este workflow pertence a um experimento antigo com um dataset CC0 de receitas."
          echo "Ele não importa dados e não representa a fonte atual do catálogo."
          echo "Use o workflow 'Importar receitas do Wikilivros' para a operação atual."
